import React from "react";
import { FileInput as FileInputComponent, FileInputProps } from "@mantine/core";

export const FileInput = (props: FileInputProps) => {
  return <FileInputComponent {...props} />;
};
