import React from "react";
import { NativeSelect as NativeSelectComponent, NativeSelectProps } from "@mantine/core";

export const NativeSelect = (props: NativeSelectProps) => {
  return <NativeSelectComponent {...props} />;
};
