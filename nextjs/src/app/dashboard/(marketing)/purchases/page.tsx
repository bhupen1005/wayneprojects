const PurchasesPage = () => {
  return (
    <div>
      <h1>List of items purchased.</h1>
    </div>
  );
};

export default PurchasesPage;


// We can group the routes using () in the folder name
// pay attention to the folder name (marketing) in the path.
// this grouping does not affect the URL, it is just a way to group the files